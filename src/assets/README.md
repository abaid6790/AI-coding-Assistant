Reserved for static assets imported directly into components (images,
icons, fonts). None exist yet — the app currently uses emoji and inline
SVG/Tailwind for all iconography, so nothing has needed this folder so far.
